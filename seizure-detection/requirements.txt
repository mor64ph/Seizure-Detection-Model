# Runtime dependencies for the Streamlit demo (app/streamlit_app.py).
#
# Lower bounds, not pins: the app uses no version-specific behaviour, and a
# hosted build resolving to newer wheels is preferable to one failing on a
# yanked pin. `pyproject.toml` remains the source of truth for the pipeline.
#
# mne is needed even though the app does no training: it decodes .edf, both for
# the raw-trace view and for the upload path.

streamlit>=1.40
plotly>=5.20
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
scipy>=1.13
mne>=1.7
pydantic>=2.7
pyarrow>=16.0
pyyaml>=6.0
joblib>=1.4
