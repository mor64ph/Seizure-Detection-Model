# seizure-detection

The pipeline package. **Project documentation is in the [repository README](../README.md)** —
installation, usage, results, limitations and attribution all live there, so this file stays a
pointer rather than a second copy that drifts out of step.

| Path | Contents |
|---|---|
| [`src/seizure/`](src/seizure/) | ingest, labels, signal, features, splits, eval, models |
| [`app/`](app/) | Streamlit application ([notes](app/README.md)) |
| [`configs/`](configs/) | experiment configurations; `base.yaml` is the reference run |
| [`artifacts/report.md`](artifacts/report.md) | full evaluation, regenerated by `make report` |
| [`samples/`](samples/) | bundled demo recording ([notes](samples/README.md)) |
| [`docs/data-notes.md`](docs/data-notes.md) | discrepancies found in the source data |
| [`../RULES.md`](../RULES.md) | 44 binding rules; read before changing pipeline behaviour |

```bash
make test          # pytest, 142 tests
make lint          # ruff
make report        # regenerate artifacts/report.md and report.json
```
